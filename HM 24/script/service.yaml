apiVersion: v1
kind: Service
metadata:
  name: random-string-service
spec:
  type: LoadBalancer
  selector:
    app: random-string
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
